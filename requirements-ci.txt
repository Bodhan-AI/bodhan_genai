# CPU-only test dependencies for GitHub Actions runners (no CUDA, no vllm,
# no flash-attn, no ray). One file for both modalities: TTS and MT share an
# environment, so CI installs one set and runs the whole suite in one job.
#
# Versions mirror constraints.txt, except torch which comes from the CPU index.
# vllm is absent: it is CUDA-only, and every module that needs it is import-guarded
# (tests/mt/test_mt_lazy_import.py asserts exactly that).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0
transformers==5.13.1
tokenizers==0.22.2
datasets==5.0.1
huggingface-hub==1.27.0
jinja2==3.1.6
accelerate==1.14.0
safetensors==0.8.0
sentencepiece==0.2.2
filelock==3.29.0
pandas==3.0.5
pyarrow==25.0.1

# TTS: snac/soundfile back the codec and audio_io WAV tests; numba backs the
# sampler test (skipped without it); fastapi + httpx back the serving-endpoint
# tests, which drive real FastAPI routing through starlette's TestClient.
snac==1.2.1
soundfile==0.14.0
pyloudnorm==0.2.0
numba==0.65.0
fastapi==0.136.3
httpx==0.28.1

# MT: trl/peft build a real SFTConfig so a TRL field rename fails here rather than
# on the cluster; sacrebleu backs BLEU and chrF++, including the test asserting
# chrF++ (word_order=2) differs from plain chrF.
trl==1.6.0
peft==0.20.0
sacrebleu==2.6.0
openai==2.53.0
# pillow backs the OCR stage-2 end-to-end test (crop -> transcribe -> assign -> assemble
# against a fake recognizer). Without it that test skips, and the seam where transcriptions
# get misaligned with their blocks is the one place OCR most needs covering. CPU-only.
pillow==12.3.0

numpy==2.3.5
pyyaml==6.0.3
# core deps not covered transitively (package is installed with --no-deps in CI)
pydantic==2.13.4
sortedcontainers==2.4.0
tqdm==4.70.0
pytest==9.1.1
# ASR: torchaudio backs audio input and feature extraction; websockets and uvicorn
# back the streaming and serving tests. Absent, seven tests/asr modules fail at
# COLLECTION, which reads as a broken branch rather than a missing dependency.
torchaudio==2.11.0
websockets==17.0.1
uvicorn==0.52.1
