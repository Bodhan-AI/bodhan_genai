"""bodhan_genai.mt.training — LoRA finetuning.

PROVISIONAL: this is a replaceable default. Nothing outside this subpackage may
import from it; the rest of bodhan_genai.mt talks to training only through the
`messages` JSONL it consumes and the PEFT adapter directory it produces.
See docs/mt/training.md.
"""
